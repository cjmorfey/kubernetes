#!groovy
// Install master
properties([disableConcurrentBuilds()])

pipeline {
    agent {
        label 'slave'
    }


    options {
        buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
        timestamps()
        ansiColor("xterm")
    }
    stages {
        stage("Create Deployment Nginx") {
            steps {
                sh 'kubectl -n webserver apply -f /root/workspace/deployments/deploy-nginx/deployments/nginx-deployment.yml -o yaml --export --ignore-not-found'
            }
        }
        stage("Create HPA Nginx"){
            steps {
                sh 'kubectl -n webserver apply -f /root/workspace/deployments/deploy-nginx/deployments/hpa_nginx.yml -o yaml --export --ignore-not-found'
            }
        }

    }
}